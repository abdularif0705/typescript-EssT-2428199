{
  "compilerOptions": {
    "outDir": "build",
    "target": "esnext", // tsconfig.json - use esnext for the version of ECMAScript u are using
    "noEmit": true
  },
  "include": ["src/**/*"] // tsconfig.json telling it to compile in which folder
}
